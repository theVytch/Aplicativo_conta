<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Define a cor de fundo -->
    <solid android:color="@color/corDeFundoComponente" />

    <!-- Define a borda -->
    <stroke
        android:width="1dp"
        android:color="@android:color/black" />
</shape>